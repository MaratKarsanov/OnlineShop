@model IEnumerable<UserViewModel>

<div class="row">
    @await Html.PartialAsync("AdministratorPanel")
    <div class="col-9">
        <h2>Список пользователей</h2>
        <a asp-area="Administrator" asp-action="Add" asp-controller="User">Добавить пользователя</a>
        @if (Model.Count() > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Имя пользователя</th>
                        <th>Телефон</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>
                                <a asp-area="Administrator" asp-action="Edit" asp-controller="User" asp-route-login="@user.UserName">
                                    @user.UserName
                                </a>
                            </td>
                            <td>@user.PhoneNumber</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>